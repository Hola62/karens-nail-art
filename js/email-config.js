// EmailJS configuration for sending emails from the frontend
// IMPORTANT: Replace the placeholders with your actual EmailJS credentials
// Create an account at https://www.emailjs.com/ and set up a service and a template

window.EMAIL_SETTINGS = {
  // e.g., 'service_xxxxxx'
  serviceId: 'REPLACE_WITH_YOUR_SERVICE_ID',
  // e.g., 'template_yyyyyy'
  templateId: 'REPLACE_WITH_YOUR_TEMPLATE_ID',
  // e.g., 'a1b2C3D4E5F6G7H8I'
  publicKey: 'REPLACE_WITH_YOUR_PUBLIC_KEY',
  // Optional: shown as sender in the email template if you set it up
  fromEmail: 'info@karensnailart.com',
  fromName: "Karen's Nail Art"
};
